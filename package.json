{
  "name": "maryampos",
  "version": "1.0.0",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "build": "electron-packager . MaryamPOS --platform=win32 --arch=x64 --out=dist --overwrite --asar=false"
  },
  "devDependencies": {
    "electron": "^33.0.0",
    "electron-packager": "^17.1.2"
  },
  "build": {
    "files": [
      "main.js",
      "backend/**/*",
      "backend/node_modules/**/*",
      "frontend/build/**/*",
      "package.json"
    ]
  }
}
